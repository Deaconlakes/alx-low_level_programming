#include <stdio.h>
/**
 * main - a funtion that prints all num in base 10
 *
 * Return: always 0
 *
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
